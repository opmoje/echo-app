# 🚀 Деплой на Render.com

Пошаговая инструкция для деплоя Instagram Chatbot на Render.com

## 📋 Подготовка

### 1. Убедитесь что проект готов к деплою

Проверьте что все файлы созданы:
- ✅ `render.yaml` - конфигурация для Render
- ✅ `.node-version` - версия Node.js
- ✅ Frontend настроен на использование переменных окружения

### 2. Закоммитьте изменения в Git

```bash
git add .
git commit -m "Prepare for Render.com deployment"
git push origin main
```

## 🌐 Деплой через Blueprint (Автоматический)

### Шаг 1: Создайте аккаунт на Render.com

1. Перейдите на https://render.com
2. Нажмите **"Get Started"**
3. Зарегистрируйтесь через GitHub (рекомендуется)

### Шаг 2: Создайте новый Blueprint

1. В Dashboard нажмите **"New +"** → **"Blueprint"**
2. Выберите ваш GitHub репозиторий `opmoje/echo-app`
3. Render автоматически найдет `render.yaml` и покажет план деплоя:
   - **instaai-backend** (Web Service)
   - **instaai-frontend** (Static Site)
4. Нажмите **"Apply"**

### Шаг 3: Настройте переменные окружения для Backend

После создания сервисов, зайдите в **instaai-backend**:

1. Откройте **Environment** в боковом меню
2. Добавьте обязательные переменные:

```
INSTAGRAM_APP_ID=ваш_facebook_app_id
INSTAGRAM_APP_SECRET=ваш_facebook_app_secret
WEBHOOK_VERIFY_TOKEN=ваш_случайный_токен_123
```

3. Проверьте автоматически созданные переменные:
   - `NODE_ENV=production`
   - `PORT` (автоматически от Render)
   - `FRONTEND_URL` (обновите URL после создания frontend)
   - `INSTAGRAM_REDIRECT_URI` (обновите URL вашего backend)

4. Нажмите **"Save Changes"**

### Шаг 4: Получите URLs ваших сервисов

После успешного деплоя вы получите URL:

**Backend:**
```
https://instaai-backend-XXXX.onrender.com
```

**Frontend:**
```
https://instaai-frontend-XXXX.onrender.com
```

### Шаг 5: Обновите переменные окружения

Вернитесь в настройки **instaai-backend** → **Environment** и обновите:

```
FRONTEND_URL=https://instaai-frontend-XXXX.onrender.com
INSTAGRAM_REDIRECT_URI=https://instaai-backend-XXXX.onrender.com/auth/callback
```

Также обновите **instaai-frontend** → **Environment**:

```
VITE_BACKEND_URL=https://instaai-backend-XXXX.onrender.com
```

После изменений Render автоматически сделает редеплой.

## 📱 Настройка Instagram/Facebook App

### Шаг 6: Обновите Redirect URIs

1. Откройте Facebook App Dashboard: https://developers.facebook.com/apps/
2. Перейдите в ваше приложение
3. **Products → Instagram → Basic Display**
4. В **OAuth Redirect URIs** добавьте:

```
https://instaai-backend-XXXX.onrender.com/auth/callback
```

5. Сохраните изменения

### Шаг 7: Настройте Webhook

1. В Facebook App → **Products → Webhooks**
2. Выберите **Instagram** в dropdown
3. Нажмите **"Edit Subscription"**
4. Заполните:
   - **Callback URL:** `https://instaai-backend-XXXX.onrender.com/webhook`
   - **Verify Token:** значение из `WEBHOOK_VERIFY_TOKEN`
5. Выберите поля подписки:
   - ☑️ `messages`
6. Нажмите **"Verify and Save"**

Если всё правильно, вы увидите ✅ рядом с URL webhook.

## ✅ Проверка деплоя

### Шаг 8: Проверьте работу приложения

1. Откройте frontend: `https://instaai-frontend-XXXX.onrender.com`
2. Нажмите **"Подключить Instagram"**
3. Пройдите OAuth авторизацию
4. После успешной авторизации вы должны увидеть статус "✅ Успешно подключено!"

### Шаг 9: Протестируйте webhook

1. Отправьте сообщение в директ вашему Instagram аккаунту с другого аккаунта
2. Бот должен автоматически ответить тем же сообщением (echo)
3. Проверьте логи в Render Dashboard → **instaai-backend** → **Logs**

## ⚠️ Важные моменты

### Холодный старт на бесплатном tier

- Backend "засыпает" после 15 минут бездействия
- Первый запрос после сна займет 30-60 секунд
- **Решение:** Перед тестированием зайдите на backend URL чтобы "разбудить" сервер

### Разбуживание backend вручную:

Просто откройте в браузере:
```
https://instaai-backend-XXXX.onrender.com/health
```

Подождите пока страница загрузится (~30-60 сек при холодном старте).

## 🔧 Troubleshooting

### Ошибка при деплое backend

**Проблема:** Build failed
**Решение:**
1. Проверьте логи в Render Dashboard
2. Убедитесь что `package.json` корректный
3. Проверьте что все зависимости установлены

### Webhook не верифицируется

**Проблема:** Webhook verification failed
**Решение:**
1. Убедитесь что `WEBHOOK_VERIFY_TOKEN` совпадает в Render и Facebook
2. Проверьте что backend доступен (откройте `/health` endpoint)
3. Проверьте логи backend

### OAuth ошибки

**Проблема:** Redirect URI mismatch
**Решение:**
1. Убедитесь что в Facebook App добавлен ваш Render URL
2. Проверьте что `INSTAGRAM_REDIRECT_URI` корректен
3. URL должны точно совпадать (включая https://)

### Frontend не может подключиться к backend

**Проблема:** CORS ошибки или network errors
**Решение:**
1. Проверьте что `VITE_BACKEND_URL` установлен правильно
2. Убедитесь что backend запущен (проверьте статус в Render)
3. Проверьте что `FRONTEND_URL` в backend настроен правильно

## 💰 Бесплатный tier ограничения

**Render.com бесплатный plan:**
- ✅ Backend: 750 часов/месяц (достаточно для одного сервиса)
- ✅ Frontend: Безлимитно (static site)
- ⚠️ Backend засыпает после 15 мин бездействия
- ⚠️ Холодный старт: 30-60 секунд

**Если нужен всегда активный сервер:**
- Upgrade to Starter Plan: $7/месяц
- Никакого холодного старта
- Больше ресурсов

## 📊 Мониторинг

### Проверка логов

**Backend:**
1. Render Dashboard → **instaai-backend** → **Logs**
2. Здесь видны все запросы и ошибки

**Frontend:**
1. Render Dashboard → **instaai-frontend** → **Logs**
2. Здесь видны только build логи

### Метрики

В Render Dashboard для каждого сервиса доступны:
- CPU usage
- Memory usage
- Bandwidth
- Request count

## 🔄 Обновление приложения

После внесения изменений в код:

```bash
git add .
git commit -m "Update: описание изменений"
git push origin main
```

Render автоматически:
1. Обнаружит изменения в GitHub
2. Запустит новый build
3. Задеплоит новую версию

## 🌍 Альтернативные регионы

По умолчанию используется регион **Oregon (US West)**.

Для изменения региона отредактируйте `render.yaml`:
```yaml
region: oregon  # или frankfurt, singapore, ohio
```

Доступные регионы:
- `oregon` (US West)
- `ohio` (US East)
- `frankfurt` (Europe)
- `singapore` (Asia)

## 📞 Поддержка

**Render.com документация:**
- https://render.com/docs

**Render.com Community:**
- https://community.render.com

**Проблемы с Instagram API:**
- https://developers.facebook.com/support/

---

**Готово! 🎉**

Ваше приложение теперь доступно онлайн и готово к использованию.
